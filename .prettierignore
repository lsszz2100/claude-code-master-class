# 빌드 산출물 — 손으로도 포매터로도 고치지 말 것.
# build_course.py + tools/prerender.mjs 가 바이트 단위로 결정적으로 만든다.
# 포매터가 재포맷하면 내용은 그대로인 채 804KB -> 1.1MB 로 부풀고 2만 줄짜리 diff 가 생긴다.
index.html
sitemap.xml

# prerender.mjs 가 챕터 높이를 잴 때 쓰는 임시 파일
.cv-measure.html

node_modules
